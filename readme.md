1) What is the difference between null and undefined?
=> undefine means a variable who's value is not define. But null means a type of undefine which show a typeOf object. These are a bug in js. If we want to declare a value undefine we use null to define variable.

2) What is the use of the map() function in JavaScript? How is it different from forEach()?
=> map() is a callback function which returns an array. Map always collect data from an array.

3) What is the difference between == and ===?
=> == means equal value and === means equal value and type. == always only compare value. === always compare value and type.

4) What is the significance of async/await in fetching API data?
=> The significance of async/await in fetching API data is that it makes asynchronous code easier to read, write, and manage compared to traditional .then() and .catch() methods.

5) Explain the concept of Scope in JavaScript (Global, Function, Block).
=> Scope means where can access the variable.
Global: Can access anywhere in js file
Function: Function scope means the variable which call from only function.
Block: block scope means access the variable if block.# assaignment-6
# assaignment-6
# restartA6
